apiVersion: v1
kind: Pod
metadata:
  name: node-selector
  labels : 
    purpose : "selector-demo"
spec:
  nodeSelector:
    topology.kubernetes.io/zone: us-east-1b # give it Particular Node and Az no worker Nodes Available in the 1-C 
  containers:
    - name: nginx
      image: nginx
      
      